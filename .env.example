# Supabase 프로젝트 설정. 실제 값은 .env.local 에 넣는다 (저장소에 올리지 않음).
#
# 주의: anon 키는 비밀이 아니다. Vite 가 번들에 그대로 박아 넣고, 사이트에 접속하는
# 모든 사람에게 내려간다. 데이터를 지키는 건 이 키가 아니라 RLS 정책이다.
# service_role 키는 절대 여기에도, 프런트 어디에도 넣지 말 것.
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon key>
